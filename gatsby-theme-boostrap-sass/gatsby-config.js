module.exports = {
  plugins: [
    {
      resolve: `gatsby-plugin-sass`,
      options: {
        precision: 6,
      },
    },
    // `boostrap`,
    // `node-sass`,
    // `reactstrap`,

    // `popper.js`,
    // `jquery`,

    // `animate.css`,


  ]
}
